import { StyleSheet } from 'react-native';

export const gStyles = StyleSheet.create({
    container: {
      flex: 1,
      padding: 20,
      paddingTop: 50,
    },
    title: {
        color: '#333',
        fontSize: 20,
        textAlign: 'center',
    }
});